[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-challenge"
version = "0.1.0"
description = "Multi-week AI learning project in Python"
requires-python = ">=3.11"
dependencies = ["flask"]
